# Global Rules (Apply to ALL projects)
   
   ## Critical Security
   - NEVER commit .env files
   - NEVER hardcode credentials
   - ALWAYS use environment variables
   
   ## Code Quality
   - All changes require tests
   - Follow GSD workflow for complex tasks
   - Atomic commits only